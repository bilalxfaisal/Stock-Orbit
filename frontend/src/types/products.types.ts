export interface ProductResponse {
    id: number,
    model: string,
    brand: string,
    quantity: number,
    price: number,
    category: string,
    productType: string,
    container: string,
}